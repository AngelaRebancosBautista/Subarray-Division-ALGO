# Subarray-Division-ALGO

/This program counts how many contiguous segments of length `m` in the list `s` have a sum equal to `d`. It iterates through all possible starting points, sums each segment of size `m`, and increments a counter whenever the sum matches `d`.
